# Never commit real cluster secrets or artifacts
*.db            # etcd snapshots
*.kubeconfig
kubeconfig
admin.conf
pki/
*.key
*.crt
*.pem
*.token

# OS / editor
.DS_Store
*.swp

# Local tooling / machine-specific config (do not publish)
.claude/settings.local.json
.superpowers/
